<?xml version="1.0" encoding="utf-8"?>

<network-security-config>


    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">192.168.100.195</domain>
        <domain includeSubdomains="true">cap-epac.local</domain>
        <domain includeSubdomains="true">*.cap-epac.local</domain>

        <!-- Faire confiance à la CA racine mkcert de CAP-EPAC -->
        <trust-anchors>
            <!-- CA système Android (certificats publics standards) -->
            <certificates src="system"/>
            <!-- Notre CA locale mkcert — fichier dans res/raw/ -->
            <certificates src="@raw/cap_epac_ca"/>
        </trust-anchors>
    </domain-config>

    <!-- ── Localhost pour Metro bundler / debug ──────────────── -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </domain-config>

    <!-- ── Configuration de base (tout le reste) ────────────── -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

</network-security-config>